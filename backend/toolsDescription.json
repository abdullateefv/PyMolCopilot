[
  {
    "type": "function",
    "function": {
      "name": "get_current_weather",
      "description": "Get the current weather in a given location",
      "parameters": {
        "type": "object",
        "properties": {
          "location": {
            "type": "string",
            "description": "The city and state, e.g. San Francisco, CA"
          },
          "unit": {
            "type": "string",
            "enum": [
              "celsius",
              "fahrenheit"
            ]
          }
        },
        "required": [
          "location"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "bg_color",
      "description": "Sets the background color of the Pymol window",
      "parameters": {
        "type": "object",
        "properties": {
          "color": {
            "type": "string",
            "description": "The color, e.g. red, blue, green, white, black, deeppurple"
          }
        },
        "required": [
          "color"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "attach_cmd",
      "description": "Attaches the described atom to the position selected on-screen",
      "parameters": {
        "type": "object",
        "properties": {
          "element": {
            "type": "string",
            "description": "The element (Symbol) to be attached e.g. O for Oxygen, H for Hydrogen"
          },
          "geometry": {
            "type": "integer",
            "description": "The geometry (steric number) of added atom e.g. 1"
          },
          "valence": {
            "type": "integer",
            "description": "The valence of the added atom e.g. 2"
          }
        },
        "required": [
          "element",
          "geometry",
          "valence"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "backward_cmd",
      "description": "Moves the movie back one frame",
      "parameters": {
        "type": "object",
        "properties": {},
        "required": []
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "button_cmd",
      "description": "Redefines what buttons do",
      "parameters": {
        "type": "object",
        "properties": {
          "button": {
            "type": "string",
            "description": "The mouse button to redefine (e.g., left, middle, right, wheel, double_left, etc.)"
          },
          "modifier": {
            "type": "string",
            "description": "The modifier key associated with the button (e.g., None, Shft, Ctrl, CtSh, etc.)."
          },
          "action": {
            "type": "string",
            "description": "The action to be performed when the button is clicked (e.g., Rota, Move, MovZ, etc.)"
          }
        },
        "required": [
          "action"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "fetch_cmd",
      "description": "Downloads a file from the internet (if possible).",
      "parameters": {
        "type": "object",
        "properties": {
          "code": {
            "type": "string",
            "description": "A single PDB identifier or a list of identifiers. Supports 5-letter codes for fetching single chains (e.g., '1a00A')."
          },
          "name": {
            "type": "string",
            "description": "The object name into which the file should be loaded."
          },
          "state": {
            "type": "integer",
            "description": "The state number into which the file should be loaded."
          },
          "type": {
            "type": "string",
            "description": "The file type to fetch. Options include cif, pdb, pdb1, 2fofc, fofc, emd, cid, sid.",
            "enum": ["cif", "pdb", "pdb1", "2fofc", "fofc", "emd", "cid", "sid"],
            "default": "cif"
          },
          "async_": {
            "type": "integer",
            "description": "Whether to download in the background and not block the PyMOL command line."
          },
          "path": {
            "type": "string",
            "description": "The path to save the downloaded file."
          }
        },
        "required": [
          "code"
        ]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "deselect_cmd",
      "description": "Disables any and all visible selections.",
      "parameters": {
        "type": "object",
        "properties": {
          }
        },
        "required": []
      }
    },
  {
    "type": "function",
    "function": {
      "name": "id_atom_cmd",
      "description": "Returns the original source ID of a single atom",
      "parameters": {
        "type": "object",
        "properties": {
          "selection": {
            "type": "string",
            "description": "Atom selection in PyMOL"
          }
        },
        "required": [
          "selection"
        ]
      }
    }
  }
]