[
  {
    "type": "function",
    "function": {
      "name": "get_current_weather",
      "description": "Get the current weather in a given location",
      "parameters": {
        "type": "object",
        "properties": {
          "location": {
            "type": "string",
            "description": "The city and state, e.g. San Francisco, CA"
          },
          "unit": {
            "type": "string",
            "enum": ["celsius", "fahrenheit"]
          }
        },
        "required": ["location"]
      }
    }
  },
    {
    "type": "function",
    "function": {
      "name": "bg_color",
      "description": "Sets the background color of the Pymol window",
      "parameters": {
        "type": "object",
        "properties": {
          "color": {
            "type": "string",
            "description": "The color, e.g. red, blue, green, white, black, deeppurple"
          }
        },
        "required": ["color"]
      }
    }
  }, 
    { 
    "type": "function",
    "function": {
        "name": "bond",
        "description": "Creates a new bond between two selections, each of which should contain one atom.",
        "parameters" : {
      "type": "object",
      "properties": {
          "atom1": 
          {
            "type": "string",
            "description": "The basic unit of matter which makes up all elements"
          },
          "atom2": 
          {
            "type": "string",
            "description": "The basic unit of matter which makes up all elements"
          }
      },
      "required": ["atom1", "atom2"]
        }
     }
    }
]